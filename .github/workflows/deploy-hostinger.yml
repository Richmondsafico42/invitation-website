name: Deploy to Hostinger

on:
  push:
    branches:
      - main
      - devin/1781067032-initial-site

permissions:
  contents: write

jobs:
  build-and-deploy:
    if: ${{ !contains(github.event.head_commit.message, '[skip ci]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build and publish to repo root
        run: npm run build

      - name: Commit built files for Hostinger Git deploy
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html assets .htaccess celebrants favicon.svg
          git diff --staged --quiet || git commit -m "Deploy built site to root [skip ci]"
          git push

      - name: Publish dist to hostinger branch
        run: |
          cd dist
          git init
          git checkout -b hostinger
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Deploy production build"
          git push -f origin hostinger
